apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-deployment-2
spec:
  selector:
    matchLabels:
      app: account-2
  replicas: 1
  template:
    metadata:
      labels:
        app: account-2
    spec:
      containers:
        - name: account-2
          image: phonglai0809/ou-social-network-account:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8083
          # env:
          #   - name: DB_HOST
          #     valueFrom :
          #       configMapKeyRef :
          #         name : mysql-config
          #         key :  host
          #   - name: DB_NAME
          #     valueFrom :
          #       configMapKeyRef :
          #         name : mysql-config
          #         key :  dbName
          #   - name: DB_USERNAME
          #     valueFrom :
          #       secretKeyRef :
          #         name : mysql-secret
          #         key :  username
          #   - name: DB_PASSWORD
          #     valueFrom :
          #       secretKeyRef :
          #         name : mysql-secret
          #         key :  password