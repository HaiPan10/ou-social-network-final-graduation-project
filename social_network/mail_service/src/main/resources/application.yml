# ConfigurationDB
spring:
  mail:
    host: cp03hn.emailserver.net.vn
    port: 465
    username: ousocialnetwork@ousocialnetwork.id.vn
    password: Vo2Z%)nF_+,{
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            protocols: TLSv1.2
            trust: "*"
        debug: true
  kafka:
    bootstrap-servers: 10.0.163.82:9092
    consumer:
      group-id: notificationId
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: "*"
            type:
              mapping: event-mailaccount:com.ou.mailservice.event.AccountMailEvent, event-mailpost:com.ou.mailservice.event.PostMailEvent
  application:
    name: mail-service
#JPA/Hibernate
server:
  error:
    include-message: always
  #port: 8084
  port: 0   
  # servlet:
  #   context-path: /social_network


#Logging
logging:
  level:
    root: info
    org:
      springframework:
        security: INFO

eureka:
  client:
    serviceUrl:
      defaultZone: http://10.0.24.90:8761/eureka
  instance:
    prefer-ip-address: true